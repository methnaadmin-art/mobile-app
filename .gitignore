# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Release secrets and signing material
android/key.properties
keystore/*.jks
keystore/*.keystore
keystore/*.p12
keystore/*.pem
*.mobileprovision

# Service config secrets (keep example files only)
android/app/google-services.json
ios/Runner/GoogleService-Info.plist

# Local release define files (keep example files only)
env/release.json
env/release.local.json

# Build / analysis logs (do not commit)
analyze_output.txt
analyze*.txt
build_output.txt
tmp_bundle_release_stacktrace.txt
hs_err_pid*.log
replay_pid*.log
java_pid*.hprof
android/hs_err_pid*.log
android/java_pid*.hprof

# Editor / agent caches
.gradle-agent/
.vscode/
tmp/
output/

# Local backups (do not commit)
*.bak
android/app/google-services.backup-*.json
android/app/google-services.bak

# Local internal helper trees not part of the mobile app
methna_backend_fix/

# Local LaTeX submission docs (keep markdown checklists in docs/)
PLAY_STORE_SUBMISSION.tex
docs/methna_full_uml.tex
